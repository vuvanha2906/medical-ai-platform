# ============================================================
#  medical-ai-platform — requirements reference
#  Python 3.12  |  managed by uv
#
#  PRIMARY: use pyproject.toml + uv
#    uv sync                      # install all deps
#    uv sync --extra dev          # + dev tools
#    uv sync --extra experiment   # + MLflow / W&B
#    uv sync --extra report       # + PDF generation
#
#  This file is a plain-text reference / fallback only.
#  For PyTorch GPU build, see comments below.
# ============================================================

# ── Web framework ────────────────────────────────────────────
django>=4.2,<5.0
djangorestframework>=3.15
django-cors-headers>=4.3
djangorestframework-simplejwt>=5.3
django-filter>=24.1

# ── Database ─────────────────────────────────────────────────
psycopg2-binary>=2.9

# ── Async task queue ─────────────────────────────────────────
celery>=5.4
redis>=5.0
django-celery-results>=2.5
django-celery-beat>=2.6

# ── Storage ──────────────────────────────────────────────────
pillow>=10.3
boto3>=1.34
django-storages>=1.14

# ── Deep learning ────────────────────────────────────────────
# CPU (dev / no GPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# GPU CUDA 12.1 (recommended for training):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# GPU CUDA 11.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# With uv + GPU:
#   uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torchvision>=0.18
# torch listed separately — installed via custom index (see above)

# ── Explainable AI ───────────────────────────────────────────
grad-cam>=1.5

# ── Medical imaging ──────────────────────────────────────────
nibabel>=5.2
nilearn>=0.10
SimpleITK>=2.3

# ── Scientific computing ─────────────────────────────────────
numpy>=1.26
scipy>=1.13
scikit-learn>=1.4
pandas>=2.2
opencv-python-headless>=4.9
matplotlib>=3.8

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0
pydantic>=2.7
httpx>=0.27
tqdm>=4.66
loguru>=0.7

# ── Dev tools (install with: uv sync --extra dev) ────────────
# pytest>=8.2
# pytest-django>=4.8
# pytest-cov>=5.0
# factory-boy>=3.3
# ruff>=0.4
# mypy>=1.10
# django-stubs>=5.0
# ipython>=8.24
# django-debug-toolbar>=4.3
# django-extensions>=3.2

# ── Experiment tracking (uv sync --extra experiment) ─────────
# mlflow>=2.13
# wandb>=0.17
# tensorboard>=2.17
# seaborn>=0.13
# plotly>=5.22

# ── Report generation (uv sync --extra report) ───────────────
# reportlab>=4.2
# weasyprint>=62.0
# jinja2>=3.1